#include <stdio.h>
#include <stdlib.h>

int main()
 {
    int a=5;
    //printf("Enter a no \x7");
     //  scanf("%d ",&a);
    if(a==5 && a!=6 && a<=60 && a>4)
    {
        printf("All the conditions are true! \a ");  // \x7 for ton we can use \a also
    }
    return 0;
}
